# Test Matrix workflow - DISABLED (old Bash version)
# Project has been migrated to Go - CI workflow handles testing
# This file is kept for reference only

name: Test Matrix (Disabled)

on:
  workflow_dispatch:  # Only manual trigger

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test Matrix disabled - project migrated to Go"
