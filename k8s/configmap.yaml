# OCTALUM-PULSE Kubernetes ConfigMap
# Configuration for pulse operations
#
# SPDX-License-Identifier: MIT
# Copyright (c) 2025 Harery

apiVersion: v1
kind: ConfigMap
metadata:
  name: pulse-config
  namespace: pulse
  labels:
    app.kubernetes.io/name: pulse
    app.kubernetes.io/component: system-maintenance
    app.kubernetes.io/part-of: pulse
    app.kubernetes.io/managed-by: kubectl
data:
  # Log level: debug, info, warn, error
  OCTALUM-PULSE_LOG_LEVEL: "info"

  # Timezone
  TZ: "UTC"

  # Maintenance schedule notes
  schedule.notes: |
    Weekly: Sundays at 2 AM
    Daily: 3 AM
    Monthly: 1st at 2 AM

  # Example configuration for different maintenance modes
  mode.full: "--auto --cleanup --purge-kernels --security-audit"
  mode.minimal: "--upgrade --quiet"
  mode.cleanup: "--cleanup --purge-kernels"
